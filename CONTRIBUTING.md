# Contributing

When opening a pull request:

1. Do not change the version in `info.json`. Version bumping is handled by the release workflow.
2. Document any user-visible change in the `Unreleased` section of `changelog.txt`.
